<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"

    android:id="@+id/half_animation_layout"
    android:layout_width="180dp"
    android:layout_height="180dp"
    android:layout_centerInParent="true"
    android:background="@drawable/background"
    android:visibility="gone">

    <ImageView
        android:id="@+id/half_img_speakAnimation"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_marginLeft="@dimen/x240"
        android:layout_marginTop="@dimen/y100"
        android:src="@drawable/animation_speaking" />

    <TextView
        android:id="@+id/half_tv_cancel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/half_img_speakAnimation"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="8dp"
        android:text="手指上滑，取消发送"
        android:textColor="#acb1c7"
        android:textSize="14sp" />
</RelativeLayout>